" Vim colorscheme (colourscheme)
" A colorscheme that is not overly complicated that is ironically named
"
" Too many colorschemes have too much stuff going on with fancy colors that I
" find hard to use. This is meant to be a bit simpler and easier to read.
" Heavily based on Jetbrains Darcula.
"
" Maintainer: RRethy

hi clear

if exists("syntax_on")
  syntax reset
endif

let g:color_name = "nottoocomplicated"


