import { Disposable } from 'vscode-languageserver-protocol';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class TypingsStatus implements Disposable {
    private _acquiringTypings;
    private _client;
    private _subscriptions;
    constructor(client: ITypeScriptServiceClient);
    dispose(): void;
    readonly isAcquiringTypings: boolean;
    private onBeginInstallTypings;
    private onEndInstallTypings;
}
export declare class AtaProgressReporter {
    private _promises;
    private _disposable;
    private _invalid;
    private statusItem;
    constructor(client: ITypeScriptServiceClient);
    dispose(): void;
    private _onBegin;
    private _onEndOrTimeout;
    private onTypesInstallerInitializationFailed;
}
