import { TextDocument } from 'vscode-languageserver-protocol';
import { WorkspaceConfiguration } from 'coc.nvim';
import Proto from '../protocol';
import { ITypeScriptServiceClient } from '../typescriptService';
export interface SuggestOptions {
    readonly enabled: boolean;
    readonly names: boolean;
    readonly paths: boolean;
    readonly completeFunctionCalls: boolean;
    readonly autoImports: boolean;
}
export default class FileConfigurationManager {
    private readonly client;
    private cachedOption;
    private requesting;
    constructor(client: ITypeScriptServiceClient);
    ensureConfigurationOptions(languageId: string, insertSpaces: boolean, tabSize: number): Promise<void>;
    ensureConfigurationForDocument(document: TextDocument): Promise<void>;
    reset(): void;
    getLanguageConfiguration(languageId: string): WorkspaceConfiguration;
    isTypeScriptDocument(languageId: string): boolean;
    enableJavascript(): boolean;
    private getFileOptions;
    private getFormatOptions;
    getCompleteOptions(languageId: string): SuggestOptions;
    removeSemicolons(languageId: string): boolean;
    getPreferences(language: string): Proto.UserPreferences;
}
