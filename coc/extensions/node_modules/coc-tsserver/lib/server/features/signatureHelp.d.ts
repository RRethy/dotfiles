import { CancellationToken, Position, SignatureHelp, TextDocument } from 'vscode-languageserver-protocol';
import { SignatureHelpProvider } from 'coc.nvim/lib/provider';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class TypeScriptSignatureHelpProvider implements SignatureHelpProvider {
    private readonly client;
    static readonly triggerCharacters: string[];
    constructor(client: ITypeScriptServiceClient);
    provideSignatureHelp(document: TextDocument, position: Position, token: CancellationToken): Promise<SignatureHelp | undefined>;
    private getActiveParmeter;
    private convertSignature;
}
