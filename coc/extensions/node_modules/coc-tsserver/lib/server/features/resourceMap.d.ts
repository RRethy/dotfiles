/**
 * Maps of file resources
 *
 * Attempts to handle correct mapping on both case sensitive and case in-sensitive
 * file systems.
 */
export declare class ResourceMap<T> {
    private readonly _normalizePath?;
    private readonly _map;
    constructor(_normalizePath?: (resource: string) => string | null);
    has(resource: string): boolean;
    get(resource: string): T | undefined;
    set(resource: string, value: T): void;
    delete(resource: string): void;
    readonly values: Iterable<T>;
    readonly keys: Iterable<string>;
    private toKey;
    private isCaseInsensitivePath;
    private readonly onIsCaseInsenitiveFileSystem;
}
export declare function isWindowsPath(path: string): boolean;
