import { CancellationToken, Hover, Position, TextDocument } from 'vscode-languageserver-protocol';
import { HoverProvider } from 'coc.nvim/lib/provider';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class TypeScriptHoverProvider implements HoverProvider {
    private readonly client;
    constructor(client: ITypeScriptServiceClient);
    provideHover(document: TextDocument, position: Position, token: CancellationToken): Promise<Hover | undefined>;
    private static getContents;
}
