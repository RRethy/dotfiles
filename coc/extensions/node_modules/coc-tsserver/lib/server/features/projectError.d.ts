import { CommandManager } from 'coc.nvim/lib/commands';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class ProjectErrors {
    private disposables;
    constructor(client: ITypeScriptServiceClient, commandManager: CommandManager);
    dispose(): void;
}
