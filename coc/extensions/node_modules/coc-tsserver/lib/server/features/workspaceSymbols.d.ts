import { CancellationToken, SymbolInformation } from 'vscode-languageserver-protocol';
import { WorkspaceSymbolProvider } from 'coc.nvim/lib/provider';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class TypeScriptWorkspaceSymbolProvider implements WorkspaceSymbolProvider {
    private readonly client;
    private readonly languageIds;
    constructor(client: ITypeScriptServiceClient, languageIds: string[]);
    provideWorkspaceSymbols(search: string, token: CancellationToken): Promise<SymbolInformation[]>;
    private static getLabel;
    private getUri;
}
