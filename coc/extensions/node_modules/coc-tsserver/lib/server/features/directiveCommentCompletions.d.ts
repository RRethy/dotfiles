import { CancellationToken, CompletionContext, CompletionItem, CompletionList, Position, TextDocument } from 'vscode-languageserver-protocol';
import { ITypeScriptServiceClient } from '../typescriptService';
export default class DirectiveCommentCompletionProvider {
    private readonly client;
    constructor(client: ITypeScriptServiceClient);
    provideCompletionItems(document: TextDocument, position: Position, _token: CancellationToken, context: CompletionContext): CompletionItem[] | CompletionList;
}
